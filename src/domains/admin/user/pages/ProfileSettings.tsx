import React from 'react';
import AccountSettings from '@/domains/admin/user/pages/settings/AccountSettings';

const ProfileSettings: React.FC = () => {
  return <AccountSettings />;
};

export default ProfileSettings; 