import React from 'react';
import AccountSettings from '@/components/admin/user/pages/settings/AccountSettings';

const ProfileSettings: React.FC = () => {
  return <AccountSettings />;
};

export default ProfileSettings; 